package com.dentallab.api.enums;

/**
 * Defines how a prosthetic work is fabricated.
 * Matches ENUM('DIGITAL', 'MANUAL', 'HYBRID') in schema.
 */
public enum BuildingTechnique {
    DIGITAL,
    ANALOG,
    HYBRID
}
